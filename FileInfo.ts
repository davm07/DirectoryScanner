/**
 * The FileInfo interface represents information about a file.
 * It contains the name of the file, its path, and its size in bytes.
 */
export interface FileInfo {
    name: string;
    path: string;
    size: number;
}